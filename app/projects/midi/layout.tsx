export const metadata = {
  title: 'MIDI Configuration',
  description: 'MIDI Footswitch Configuration Tool',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return <div className='mx-auto'>{children}</div>
}
